# Neon pooled PostgreSQL connection string (server-only)
DATABASE_URL=

# Enable Neon Auth in the Neon Console, then copy its branch endpoint here.
NEON_AUTH_BASE_URL=

# Generate a random secret of at least 32 characters for signed session cookies.
NEON_AUTH_COOKIE_SECRET=
