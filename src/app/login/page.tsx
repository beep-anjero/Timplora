import Link from"next/link";import{signIn}from"./actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,var(--accent-soft),var(--surface)_45%)] p-5"><section className="panel w-full max-w-md p-7 sm:p-9"><Link href="/" className="mb-8 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[var(--brand)] text-lg font-black text-white">T</span><span className="font-display text-xl font-bold">Timplora</span></Link><p className="eyebrow">Welcome back</p><h1 className="mt-4 font-display text-3xl font-bold">Sign in to your workspace</h1><p className="mt-2 text-sm text-[var(--muted)]">Use the account provided by your manager.</p>{error&&<p role="alert" className="mt-5 rounded-xl bg-[var(--danger-soft)] p-3 text-sm font-semibold text-[var(--danger)]">{error}</p>}<form action={signIn} className="mt-6 space-y-4"><div><label className="label" htmlFor="email">Email</label><input className="field" id="email" name="email" type="email" autoComplete="email" required/></div><div><label className="label" htmlFor="password">Password</label><input className="field" id="password" name="password" type="password" autoComplete="current-password" required/></div><button className="button button-primary w-full">Sign in</button></form><p className="mt-6 text-center text-xs text-[var(--muted)]">Demo navigation remains available when Neon is not configured.</p></section></main>}
