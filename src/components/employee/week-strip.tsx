import{weekDays,shifts}from"@/lib/mock-data";export function WeekStrip(){return <div className="grid grid-cols-7 gap-2 overflow-x-auto pb-2">{weekDays.map((day,i)=>{const shift=shifts.find(s=>new Date(s.start).getDate()===Number(day.date));return <div key={day.day} className={`min-w-24 rounded-2xl border p-3 ${i===0?"border-[var(--brand)] bg-[var(--brand)] text-white":"border-[var(--line)] bg-white"}`}><p className={`text-xs font-bold ${i===0?"text-white/70":"text-[var(--muted)]"}`}>{day.day}</p><p className="mt-1 text-xl font-bold">{day.date}</p><p className={`mt-4 text-[.68rem] font-semibold ${i===0?"text-white":"text-[var(--brand)]"}`}>{shift?shift.label:"Rest day"}</p></div>})}</div>}
