import{NextResponse,type NextRequest}from"next/server";import{hasNeonAuth}from"@/lib/neon/config";import{getAuth}from"@/lib/neon/auth";export async function proxy(request:NextRequest){if(!hasNeonAuth())return NextResponse.next();return getAuth().middleware({loginUrl:"/login"})(request)}export const config={matcher:["/employee/:path*","/manager/:path*","/api/auth/:path*"]}
