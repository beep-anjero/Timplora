import{PageHeading}from"@/components/ui/page-heading";import{ScheduleBuilder}from"@/components/manager/schedule-builder";import{listEmployees}from"@/lib/data/employees";import{getSchedule}from"@/lib/data/schedules";export default async function SchedulePage(){const[employees,schedule]=await Promise.all([listEmployees(),getSchedule("2026-09-28","all")]);return <><PageHeading eyebrow="Weekly planner" title="September 28 – October 4" description="Assign shifts, resolve availability conflicts, then publish the final schedule."/><ScheduleBuilder employees={employees} initialShifts={schedule.shifts} initialStatus={schedule.status}/></>}
