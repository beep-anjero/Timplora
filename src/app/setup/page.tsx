import Link from"next/link";import{redirect}from"next/navigation";import{hasNeonDatabase}from"@/lib/neon/config";import{getSql}from"@/lib/neon/db";import{createFirstManager}from"./actions";export default async function Setup({searchParams}:{searchParams:Promise<{error?:string}>}){if(hasNeonDatabase()){const rows=await getSql()`select count(*)::int as count from profiles`;if(Number(rows[0].count)>0)redirect("/login")}const{error}=await searchParams;return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,var(--accent-soft),var(--surface)_45%)] p-5"><section className="panel w-full max-w-md p-7 sm:p-9"><Link href="/" className="mb-8 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[var(--brand)] text-lg font-black text-white">T</span><span className="font-display text-xl font-bold">Timplora</span></Link><p className="eyebrow">One-time setup</p><h1 className="mt-4 font-display text-3xl font-bold">Create the first manager</h1><p className="mt-2 text-sm text-[var(--muted)]">This page locks automatically after the first account is created.</p>{error&&<p role="alert" className="mt-5 rounded-xl bg-[var(--danger-soft)] p-3 text-sm font-semibold text-[var(--danger)]">{error}</p>}<form action={createFirstManager} className="mt-6 space-y-4"><div><label className="label" htmlFor="name">Full name</label><input className="field" id="name" name="name" autoComplete="name" required/></div><div><label className="label" htmlFor="email">Email</label><input className="field" id="email" name="email" type="email" autoComplete="email" required/></div><div><label className="label" htmlFor="password">Password</label><input className="field" id="password" name="password" type="password" minLength={8} autoComplete="new-password" required/></div><button className="button button-primary w-full">Create manager account</button></form></section></main>}
