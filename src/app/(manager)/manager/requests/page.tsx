import{PageHeading}from"@/components/ui/page-heading";import{RequestQueue}from"@/components/manager/request-queue";import{listEmployees}from"@/lib/data/employees";import{listRestDayRequests}from"@/lib/data/requests";export default async function ManagerRequests(){const[requests,employees]=await Promise.all([listRestDayRequests("all"),listEmployees()]);return <><PageHeading eyebrow="First come, first served" title="Rest day requests" description="Requests are ordered by server submission time. Priority informs review, while final approval remains with you."/><RequestQueue initial={requests} people={employees}/></>}
