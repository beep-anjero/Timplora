export type ScheduleEntry={employeeId:string;date:string;value:string};export type AvailabilityBlock={employeeId:string;weekday:number;start:string;end:string;label:string};export type ApprovedRestDay={employeeId:string;date:string};export type ScheduleConflict={employeeId:string;date:string;kind:"class"|"rest-day"|"invalid";message:string};
function overlaps(aStart:string,aEnd:string,bStart:string,bEnd:string){return aStart<bEnd&&bStart<aEnd}
export function validateSchedule(entries:ScheduleEntry[],classes:AvailabilityBlock[],restDays:ApprovedRestDay[]){const conflicts:ScheduleConflict[]=[];for(const entry of entries){if(entry.value==="Rest")continue;const parts=entry.value.split("–");if(parts.length!==2){conflicts.push({employeeId:entry.employeeId,date:entry.date,kind:"invalid",message:"Shift time is invalid."});continue}const[start,end]=parts;const rest=restDays.some(r=>r.employeeId===entry.employeeId&&r.date===entry.date);if(rest)conflicts.push({employeeId:entry.employeeId,date:entry.date,kind:"rest-day",message:"Shift conflicts with an approved rest day."});const weekday=new Date(`${entry.date}T12:00:00Z`).getUTCDay()||7;for(const block of classes.filter(c=>c.employeeId===entry.employeeId&&c.weekday===weekday)){const normalizedEnd=end<=start?"24:00":end;if(overlaps(start,normalizedEnd,block.start,block.end))conflicts.push({employeeId:entry.employeeId,date:entry.date,kind:"class",message:`Shift overlaps ${block.label}.`})}}return conflicts}
