import{PageHeading}from"@/components/ui/page-heading";import{AvailabilityForm}from"@/components/employee/availability-form";import{getCurrentClasses,getCurrentEmployee}from"@/lib/data/employees";export default async function AvailabilityPage(){const[classes,person]=await Promise.all([getCurrentClasses(),getCurrentEmployee()]);return <><PageHeading eyebrow="Working student" title="Availability" description="Keep your class schedule and shift preference current so conflicts can be caught before publishing."/><AvailabilityForm initialClasses={classes} initialPeriod={person.preferredPeriod}/></>}
